language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - python example.py
  - python -m tests.test_api
  - python -m tests.test_make_request_bodies